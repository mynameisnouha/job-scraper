"""Job sources that return the normalized record `score_jobs.py` already expects.

The older sources (LinkedIn, CareersFuture) still live in `scraper.py`; new ones
land here as one module per source.
"""
